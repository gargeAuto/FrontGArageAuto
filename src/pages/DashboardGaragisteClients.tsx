import SearchCustomer from "../component/SearchCustomer.tsx";
import {Box} from "@mui/material";

const DashboardGaragisteClients = ({}) => {

    return (
        <>
            <Box padding={20}>
            <div>Ceci est la page client</div>
            <SearchCustomer/></Box>
        </>
    );
};

export default DashboardGaragisteClients;
